on: push

jobs:
  archive-build-artifacts:
    runs-on: ubuntu-latest
    defaults:
      run: 
        working-directory: .
    steps:

      - name: update public_html
        run: |
          ls -a
          git ftp push -u "barnabe.geffroy" -p ${{ secrets.ftp_password }} "sftp://pedago-ext.ensiie.fr/~/public_html/projet-web"